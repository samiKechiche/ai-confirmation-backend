# Test environment -- used by `npm test` (Jest)
# Separate database so tests never touch your development data.
# Create it once with the SQL block in the instructions below.
PORT=3001
NODE_ENV=test
DATABASE_URL="postgresql://ai_confirmation_user:password@localhost:5432/aiconfirmation_test?schema=public"
SMS_SERVICE_URL=http://localhost:4000
VOICE_SERVICE_URL=http://localhost:5000
API_KEY=test-api-key-2026